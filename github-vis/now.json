{
  "name": "github-vis",
  "version": 2,
  "env": {
    "GITHUB_TOKEN": "@github-token"
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(?!api/?)(.*)", "dest": "/index.html" }
  ]
}
